By default just show a None template and empty note editor if navigatign to note generator and the selected template is null OR there is no collected information.
since this will be the default -- can remove that option from the settings 
