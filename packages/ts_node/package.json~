{
  "name": "tidyscripts_node",
  "version": "1.0.0",
  "description": "Common (can run on browser and web) code/apis for typescript projects",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc ./src/index.ts --esModuleInterop --declaration  --target es2015 --module commonjs --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sheun Aluko",
  "license": "ISC",
  "dependencies": {
    "extract-zip": "^2.0.1",
    "node-fetch": "^3.2.10",
    "puppeteer": "^19.0.0",
    "tidyscripts_common": "*"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}
