
/* 
   Evm wallet API for tidyscripts 
   Sun Dec  5 10:59:58 CST 2021
*/

import * as io from "../../io" 
import * as cutil from "../../../common/util/index" 

let log = (x:any) => cutil.Logger("evm") 

/* 
   
*/ 
let EVM_CONFIG_FNAME = Deno.env.get("EVM_CONFIG_FNAME")
let EVM_CONFIG_PASSW = Deno.env.get("ENV_CONFIG_PASSW")


let encrypted_config = io.read_text(EVM_CONFIG_FNAME as string)
let decrypted_config = 
